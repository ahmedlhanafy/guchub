apiVersion: apps/v1
kind: Deployment
metadata:
  name: guchub10 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: guchub10
  template:
    metadata:
      labels:
        app: guchub10
    spec:
      containers:
        - name: guchub 
          image: reviewappregistery.azurecr.io/guchub
          ports:
          - containerPort: 80